---
description: Run all QA tests using Playwright
---

# Run QA Tests

1. Install dependencies (if needed)
// turbo
2. Run Playwright tests
   ```bash
   npx playwright test
   ```

3. (Optional) Show report if tests fail
   ```bash
   npx playwright show-report
   ```
